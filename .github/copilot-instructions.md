# Webnovel Converter

## Current state

This workspace is currently empty and has no established language, framework, build command, or test suite.

## Working guidance

- Before implementing features, confirm the intended input formats, output formats, and runtime environment.
- Preserve the project's chosen language and framework once they are introduced; do not add dependencies without a clear need.
- Prefer small, focused changes and add tests alongside behavior that can be tested automatically.
- Use PowerShell-compatible commands when documenting local workflows on Windows.
- Do not commit generated files, credentials, downloaded content, or personal configuration.